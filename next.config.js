module.exports = {
  // Allow loading dev/HMR resources when testing from other devices on the LAN
  // (e.g. a phone hitting your machine's IP). Dev-only; ignored in production.
  allowedDevOrigins: ["192.168.178.36"],
};
